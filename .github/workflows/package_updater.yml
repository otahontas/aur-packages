name: Update packages

on:
  workflow_dispatch:

jobs:
  update_packages:
    name: Check for package updates, update if needed
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository and submodules
      uses: actions/checkout@v2
      with:
        ssh-key: ${{ secrets.SSH_PRIVATE_KEY }}
